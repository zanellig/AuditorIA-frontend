export interface ServerUserData {
  username: string
  email: string
  full_name: string
  is_active: boolean
}

export interface UserData {
  userEmail: string
  username: string
  userFullName: string
  isActive: boolean
}
